import { NexDirective } from './nex.directive';

describe('NexDirective', () => {
  it('should create an instance', () => {
    const directive = new NexDirective();
    expect(directive).toBeTruthy();
  });
});
